import { prism } from 'mdx-deck/themes';

import './index.css';

const theme = {
  googleFont: 'https://fonts.googleapis.com/css?family=Lato',
  fonts: {
    body: 'Lato, sans-serif',
    monospace: '"Lato Mono", monospace',
  },
};

export const defaultThemes = [/* prism, */ theme];
